<div id="feed-header">
    <nav class="top-bar" data-topbar>
	<ul class="title-area">
	    <li class="name">
		<h1>
		    <a href="<%= sub_url(@feed) %>" id="feed-title" target="_blank">
			<img id="favicon" src="<%= favicon_for(sub_url(@feed)) %>"/>
			<%= sub_title(@feed) %>
		    </a>
		</h1>
	    </li>
	    <li class="toggle-topbar menu-icon"><a href="#"><span></span></a></li>
	</ul>
	<section class="top-bar-section">
	    <ul class="right controls">
		<%= render 'controls' %>
	    </ul>
	</section>
    </nav>
</div>
<script>
var img = document.getElementById("favicon");
img.onerror = function() {
	img.setAttribute("src", "<%= asset_path('feed-icon-16x16.png') %>");
}

var fh = document.getElementById("feed-header");
var mq = window.matchMedia("(min-width: 40.063em)");
if (!mq.matches) {
//	fh.className = "fixed";
}
</script>
