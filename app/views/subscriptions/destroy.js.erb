$("#feed-controls").html("<%= escape_javascript(render('feeds/controls')) %>");
if (<%= @user.subscriptions.any? %>)
    $("#next").attr('href', '<%= feed_path(@user.next_feed) %>');
else
    $("#next").attr('href', '<%= @user %>');
$(document).foundation();
